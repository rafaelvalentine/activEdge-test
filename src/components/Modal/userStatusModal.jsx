import React from 'react'
import { Modal } from 'react-bootstrap'
// import { } from '../Card'

const Modal = ({ open, inputs, history, loading, ...props }) => {
  return (
    <Modal id='centeredmodal' centered {...props}>
      
    </Modal>
  )
}


export default Modal
